<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/tv_notificaciones"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/notificaciones"
        android:textSize="18sp" />
    <Switch
        android:id="@+id/switch_notificaciones"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="15dp"
        android:backgroundTint="?attr/colorPrimary"
        android:trackTint="?attr/colorPrimary"
        android:text="@string/activar_notificaciones"
        android:checked="true" />

    <!-- En tu layout de configuración o ajustes -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Otros ajustes que puedas tener... -->

        <!-- TU BOTÓN DE PRIVACIDAD (SIN CAMBIOS) -->
        <TextView
            android:id="@+id/privacy_settings_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Configuración de privacidad"
            android:textSize="18sp"
            android:padding="12dp"
            android:background="?attr/selectableItemBackground"
            android:clickable="true"
            android:focusable="true"
            android:drawableStart="@drawable/ic_privacy_tip_24"
            android:drawablePadding="16dp"/>

        <!-- ¡NUEVO TEXTVIEW AÑADIDO! -->
        <!-- Este es el mensaje descriptivo que aparecerá debajo del botón. -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/solo_uni_europea"
            android:textSize="11sp"
            android:textColor="?android:attr/textColorSecondary"
            android:paddingStart="52dp"
            android:paddingEnd="12dp"/>

    </LinearLayout>






</LinearLayout>
